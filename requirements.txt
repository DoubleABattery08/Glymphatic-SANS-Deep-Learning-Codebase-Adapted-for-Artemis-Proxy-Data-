# Runtime and development dependencies, pinned for reproducibility.
# Tested with CPython 3.13.2 on Windows (win32 10.0). The model runs on CPU;
# no GPU is required.

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
matplotlib==3.10.9
torch==2.12.0
requests==2.34.2

# Formatting and linting (enforced style, see pyproject.toml).
black==26.5.1
ruff==0.15.16
